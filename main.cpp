#include "io.h"
#include "do.h"

int main()
{
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    writeAnswer(1);
    // Return statement before cout
    return 0;

}